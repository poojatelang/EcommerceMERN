import React from "react";
import { Typography, Box } from "@mui/material";

const SalesReport = () => {
  return (
    <Box p={4} mt={27}>
      <Typography variant="h4">Sales Report</Typography>
      <Typography>View sales data and reports here.</Typography>
    </Box>
  );
};

export default SalesReport;
