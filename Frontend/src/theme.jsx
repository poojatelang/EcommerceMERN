import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff4400', // Your primary color
    },
    secondary: {
      main: '#0044ff', // Your secondary color
    },
  },
  typography: {
    // Customize typography here
  },
});

export default theme;
